# star-wars-generations-api

## Project setup
```
npm install
```

### Commands:
* `npm run serve` - runs the api
* `npm run test` - runs the api tests in watch mode
* `npm run test:ci` - runs the api tests for CI environments